/*if(j != 0 && num < pos[j-1]){ //Sorting array while taking input
	            for(int k = j-1; k>=0; --k){
	                if(num < pos[k]){
	                    pos[k+1] = pos[k];
	                    pos[k] = num;
	                } else{
	                    break;
	                }
	            }
	        } else{
	            pos[j] = num;
	        }*/